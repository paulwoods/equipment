# sanitized Caddyfile copied from the vm - 2026-06-10

{
	email {$CADDY_EMAIL}
}

# The app, on equipment.mrpaulwoods.com since 2026-07-19.
{$CADDY_DOMAIN} {
	encode zstd gzip

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
	}

	handle /api/* {
		reverse_proxy backend:8080
	}

	handle {
		reverse_proxy frontend:80
	}
}

# Apex placeholder, reserved for a personal site. Exists so Caddy keeps the apex
# certificate renewed; swap the `respond` for a root/file_server when that site
# lands. Deliberately not a redirect to the app.
{$CADDY_APEX_DOMAIN} {
	encode zstd gzip
	respond "Coming soon" 200
}
