events {}

http {
    server {
        listen 80;
        server_name equipment.mrpaulwoods.com;

        # Certbot webroot challenge
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        # Redirect all other HTTP traffic to HTTPS
        location / {
            return 301 https://$host$request_uri;
        }
    }

    # TEMPORARY: commented out for initial cert bootstrap — restore after certbot succeeds
    # server {
    #     listen 443 ssl;
    #     server_name equipment.mrpaulwoods.com;
    #
    #     ssl_certificate /etc/letsencrypt/live/equipment.mrpaulwoods.com/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/equipment.mrpaulwoods.com/privkey.pem;
    #
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;
    #
    #     location / {
    #         proxy_pass http://equipment-management:3000;
    #
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}
